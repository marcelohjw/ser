#include <stdio.h>

int main(void) {
    char quadrilhas, notas;
    int i, question = 0;

    for(question <= 2; question++;){
        printf("Digite quantas quadrilhas irão participar:\n");
        scanf("%c", &quadrilhas);
    }
    while(i <= 3) {
        scanf("%c", &notas);
    }
    printf("Notas: %s", &notas);
    return 0;

}